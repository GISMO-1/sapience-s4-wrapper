# AI agents

The AI service uses a deterministic rules engine to translate text into intent tool calls. It is intentionally simple to avoid external dependencies while the SAP integration is stubbed.
