# Policy-as-code module

Purpose: Provide policy-as-code components (schema, loader, evaluator, explain helpers) for the API gateway. Inputs are YAML policy documents and intents; outputs are deterministic policy decisions and explainable metadata.

Example command:
```bash
pnpm --filter api-gateway test
```

Self-check:
```bash
pnpm --filter api-gateway test
```
