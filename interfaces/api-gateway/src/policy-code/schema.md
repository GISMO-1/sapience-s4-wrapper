# Policy schema

Purpose: Define the strict Zod schema for YAML policy documents, ensuring validated structure and constraint parameters. Inputs are parsed YAML objects; outputs are validated policy documents.

Example command:
```bash
pnpm --filter api-gateway test -- -t "policy"
```

Self-check:
```bash
pnpm --filter api-gateway test -- -t "policy"
```
